#!/usr/bin/env ruby

require 'rubygems'

$LOAD_PATH.unshift(File.dirname(__FILE__) + '/../lib')

require 'wren'
require 'wren/command'

args = ARGV.dup
ARGV.clear
command_str = args.shift.strip rescue 'help'

Wren::Command.run(command_str, args)

